<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="eBuildingBlocks.Infrastructure" Version="5.0.0" />
    <!-- In-memory provider so `dotnet run` works with zero external services.
         Swap for Microsoft.EntityFrameworkCore.SqlServer (or another provider) for real persistence. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" Version="10.0.5" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\eBlocksApi.Domain\eBlocksApi.Domain.csproj" />
  </ItemGroup>

</Project>
